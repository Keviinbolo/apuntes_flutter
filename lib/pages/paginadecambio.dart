import 'package:flutter/material.dart';

class Paginadecambio extends StatelessWidget {
  const Paginadecambio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}